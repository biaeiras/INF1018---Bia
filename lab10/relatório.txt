Na primeira questão para traduzir a função add para Assembly, primeiro foi montada a RA deixando o valor a ser subtraido de rsp (  valor que vai guardar apara a RA) vaziopara ser inserido posteriomente depois de saber resgistrados armazenados na RA. 
Para variavel a foi utilizado o registrados %ebx, visto que a é inteiro armazenando 4 bytes e %ebx é um registrador calee saved e eu queria testar e praticar a salvar o valor de ebx na RA e depois restaurar devolvendo o valor original. 
O  for foi decomposto em While que é mais fácil traduzir, ficando : 
#while(x != NULL), em que null é um ponteiro pro endereço vazio, assim foi utilizado o comparador de ponteiro cmpq 

Para a+= x -> val, foi utilizado o conhecimento aprendido sobre structs, como val é o primeiro valor da estrutura ele está na primeira posição zero, assim foi aidiconado o valor em %rdi ( registrador usado por ser o primeiro argumento de 32 bits), adicionando na variável a (%ebx)

Para x->next foi utilizada uma lógica parecida, como next é um ponteiro, possui 8bytes, sendo essa sua posição 

Saindo do While foi passado o valor de a (%ebx) para o registrador da %eax com o resultado da função, além de restaurado o oeprador calee saved rbx e apagado a RA. 

Para segunda questão foi criado o RA e reservado espaço na pilha. 
A condição if (x == NULL) foi traduzida com cmpq comparando o ponteiro com 0, parecido com a questão anteior. Para  o caso recursivo o valor de x-> val foi armazenado em %ebx por ser um inteiro e o ponteiro no %rdi. Após o call, o valor de %eax foi somado com %ebx e o resultado final foi colocado em %eax, além de restaurar o registrador callee-saved %rbx e desmontar a pilha. 


Para a implementação da função boo da questão , foi utilizado um laço wile descrecescendo de um valor n passado na  função. Os parâmetros foram preservados usando registradores calee-saved como %r12, %r13 e %r14 para que não fosse perdido os valores durante a chamada da funão call
A chamada da função f(px->val1, val) respeitou as condições dos registores para cara parâmetro e tipo, usando %edi e %esi respectivamente. E ao fim de cada iteração, o ponetiro px foi incrementado de 8 bytes, que é o tamaho da struct visto que contém 2 inteiros. No final, os registrados foram restaurados e a pilha desmontada. 

A última questão, a implementação map2 foi realizada acompanhando o desenvolvimento feito em sala de aula com o professor. A função pecorre um vetor apicando a função f a cada elemento e armazenando o resultado em outro vetor. Como nos outros exercícios foi tulizado o laço while,neste caso específico para iterar sobre os índices. 